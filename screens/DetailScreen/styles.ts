import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  titleStyle: {
    textAlign: 'center',
    fontWeight: '700',
    marginTop: 24,
    fontSize: 28
  }
})

export default styles
